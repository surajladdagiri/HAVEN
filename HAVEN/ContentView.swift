import SwiftUI
/*
// MARK: - App Home Screen
struct ContentView: View {
    
    
    var body: some View {
        NavigationStack {
            List {
                // Section for your working algorithms
                Section(header: Text("Algorithms")) {
                    NavigationLink(destination: ARKitV1View()) {
                        HStack {
                            Image(systemName: "1.circle.fill")
                                .foregroundColor(.blue)
                                .font(.title2)
                            
                            VStack(alignment: .leading) {
                                Text("ARKit")
                                    .font(.headline)
                            }
                        }
                        .padding(.vertical, 4)
                    }
                    
                    NavigationLink(destination: ARKitV2View()) {
                        HStack {
                            Image(systemName: "2.circle.fill")
                                .foregroundColor(.blue)
                                .font(.title2)
                            
                            VStack(alignment: .leading) {
                                Text("ARKit SLAM")
                                    .font(.headline)
                            }
                        }
                        .padding(.vertical, 4)
                    }
                    
                    
                    NavigationLink(destination: MLV1View()) {
                        HStack {
                            Image(systemName: "3.circle.fill")
                                .foregroundColor(.blue)
                                .font(.title2)
                            
                            VStack(alignment: .leading) {
                                Text("Apple ML Model")
                                    .font(.headline)
                            }
                        }
                        .padding(.vertical, 4)
                    }
                    
                    NavigationLink(destination: ARKitVisualView(bleManager: blemanager)) {
                        HStack {
                            Image(systemName: "4.circle.fill")
                                .foregroundColor(.blue)
                                .font(.title2)
                            
                            VStack(alignment: .leading) {
                                Text("ARKit SLAM visualization")
                                    .font(.headline)
                            }
                        }
                        .padding(.vertical, 4)
                    }

                }
                
            }
            .navigationTitle("HAVEN")
        }
    }
}




#Preview {
    ContentView()
}

*/
